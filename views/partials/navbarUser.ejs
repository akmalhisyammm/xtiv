<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com?plugins=forms"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css?family=Karla:400,700&display=swap');
        .font-family-karla { font-family: karla; }
        .bg-sidebar { background: #3d68ff; }
        .cta-btn { color: #3d68ff; }
        .upgrade-btn { background: #1947ee; }
        .upgrade-btn:hover { background: #0038fd; }
        .active-nav-link { background: #1947ee; }
        .nav-item:hover { background: #1947ee; }
        .account-link:hover { background: #3d68ff; }
    </style>
    <title><%= title %></title>
</head>

<body>
    <header class="flex flex-wrap sm:justify-start sm:flex-nowrap w-full bg-gray-100 text-sm py-4">
      <nav class="container mx-auto px-4 sm:flex sm:items-center sm:left-0">
        <p class="flex-none text-xl font-semibold">Xtiv</p>
        <div class="flex flex-row items-center gap-5 mt-5 sm:justify-end sm:mt-0 sm:ps-5">
          <a href="/posts" class="font-medium text-gray-600 hover:text-gray-400">Beranda</a>
          <a href="/users/id" class="font-medium text-gray-600 hover:text-gray-400">Profile</a>
          <a href="/posts/create" class="font-medium text-gray-600 hover:text-gray-400">Post</a>
          <a href="" class="font-medium text-gray-600 hover:text-gray-400">Logout</a>
        </div>
      </nav>
    </header>