<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com?plugins=forms"></script>
  <title><%= title %> | Xtiv</title>
</head>

<body>
  <header class="bg-blue-100">
    <nav class="container flex justify-between items-center mx-auto p-4">
      <h1 class="text-2xl font-semibold">Xtiv</h1>

      <div class="flex items-center gap-6">
        <% if (user) { %>
        <a href="/posts">
          Posts
        </a>
        <a href="/users/<%= user.id %>">
          Profile
        </a>
        <a href="/logout" type="button" class="py-3 px-4 w-fit inline-flex items-center gap-x-2 text-sm font-semibold rounded-lg border border-transparent bg-red-500 text-white hover:bg-red-600 disabled:opacity-50 disabled:pointer-events-none">
          Logout
        </a>
        <% } else { %>
        <a href="/login" type="button" class="py-3 px-4 w-fit inline-flex items-center gap-x-2 text-sm font-semibold rounded-lg border border-transparent bg-blue-600 text-white hover:bg-blue-700 disabled:opacity-50 disabled:pointer-events-none">
          Login
        </a>
        <a href="/register" type="button" class="py-3 px-4 w-fit inline-flex items-center gap-x-2 text-sm font-semibold rounded-lg border border-transparent bg-teal-500 text-white hover:bg-teal-600 disabled:opacity-50 disabled:pointer-events-none">
          Register
        </a>
        <% } %>
      </div>
    </nav>
  </header>

  <main class="container mx-auto p-4">
    <h1 class="text-3xl font-semibold"><%= title %></h1>